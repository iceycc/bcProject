import API from '@/service'
import {PageName, imgSrc, LsName, BusName} from "@/Constant";
import Bus from "@/plugin/bus";

export default  {
 method:{
   /**
    * 判断该用户在比财的实名认证状态
    */
   checkUserStatusInBicai(){

   }

 }
}
